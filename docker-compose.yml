services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: nutrivoice
      POSTGRES_PASSWORD: nutrivoice
      POSTGRES_DB: nutrivoice
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
